import serial_asyncio
import asyncio
from app.comms.IRIS import iris, iris_status

class Serial:

    def serialHardwareParsePacket(iris: iris, packet: irispacket) -> iris_status:
    
    def serialHardwareSendPacket(iris: iris, packet: irispacket) -> iris_status:

    

    
